# Статус проекта "Heavy Render Benchmark"

**Дата фиксации:** `2026-01-21`
**Цель:** Зафиксировать рабочее состояние MVP (Минимально жизнеспособного продукта) перед внесением сложных изменений в логику измерений.

## 🏗️ Архитектура

Работает **4 независимых сервиса**, развернутых локально:
1.  **Backend (FastAPI):** `http://localhost:8000`
    *   **Эндпоинт:** `POST /api/benchmark/start`
    *   **Генерация данных:** Конфигурируется (`payloadSize`, `complexity`).
    *   **CORS:** Настроен для фронтендов на портах 5173, 5174, 5175.
2.  **Frontend (React):** `http://localhost:5173`
3.  **Frontend (Vue 3):** `http://localhost:5174`
4.  **Frontend (Svelte):** `http://localhost:5175`

Все фронтенды обращаются к одному бэкенду за идентичными данными.

## 📦 Технологический стек

*   **Бэкенд:** Python 3.12, FastAPI, Pydantic, Uvicorn
*   **Фронтенды:** Node.js 22.x, Vite, Axios
    *   React (с хуками)
    *   Vue 3 (Composition API)
    *   Svelte (SvelteKit skeleton)
*   **ОС:** Ubuntu 24.04 LTS

## ✅ Реализованный функционал (MVP)

### Бэкенд
- [x] Генерация тестовых данных с тремя уровнями сложности (`low`, `medium`, `high`).
- [x] Валидация входящих запросов (ограничение `payloadSize: 1-5000`).
- [x] Настройка CORS для локальной разработки.

### Фронтенды (каждое приложение)
- [x] Базовая структура проекта (Vite).
- [x] Интерфейс с кнопкой запуска теста.
- [x] Отправка POST-запроса к бэкенду с параметрами:
    ```json
    { "framework": "...", "payloadSize": 1500, "complexity": "high" }
    ```
- [x] Отображение метрик:
    - Время получения данных (Network Time)
    - Время рендеринга (Render Time) — **ТРЕБУЕТ УТОЧНЕНИЯ (см. ниже)**
    - Общее время (Total Time)
    - Количество элементов
- [x] Отображение первых 5 элементов полученного массива в формате JSON.

## ⚠️ Известные проблемы и ограничения (Текущая версия)

1.  **~~Неточный замер времени рендеринга~~** — **РЕШЕНО для React и Vue.** Реализована система замера на основе `requestAnimationFrame`.
2.  **~~Отсутствие метрики FPS~~** — **РЕШЕНО для React и Vue.** Добавлен расчёт среднего FPS и график в реальном времени.
3.  **~~Данные не отрисовываются в реальном DOM~~** — **РЕШЕНО для React и Vue.** Данные отображаются в виде полноценной HTML-таблицы (2500 строк), что создаёт реальную нагрузку.
4.  **Нет режима "Оптимизированный рендеринг"** — Все тесты выполняются в режиме "из коробки".

## 📊 Последние полученные метрики (Финальный тест)

*Параметры теста: `payloadSize=2500`, `complexity='high'`, рендеринг таблицы из 2500 строк.*

| Фреймворк | Время получения данных | Время рендеринга (тяжёлый DOM) | Средний FPS | График FPS | Ключевое наблюдение |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **React 18** | 124.00 мс | **478.00 мс** | **4.2** | Красный (блокировка) | Наибольшее время рендеринга, критически низкий FPS |
| **Vue 3** | 126.30 мс | **61.80 мс** | **16.2** | Оранжевый (просадка) | **Оптимальный баланс:** самое быстрое время, приемлемый FPS |
| **Svelte** | 125.90 мс | **252.00 мс** | **35.0** | Зелёный (плавно) | **Лучшая отзывчивость:** высокий FPS при среднем времени |

### 🎯 Итоговый вывод
Проект достиг основной цели: создан инструмент, который **количественно и наглядно** показал разницу в производительности. Результаты демонстрируют **архитектурный компромисс** — Vue быстрее всего отрисовывает результат, Svelte сохраняет интерфейс максимально отзывчивым, а React в данной конфигурации показал наихудшие результаты по обеим метрикам.


## 🚀 Следующие шаги (План улучшений)

1.  ✅ **Реализация точного замера рендеринга и FPS** — Использование `requestAnimationFrame`. *Реализовано для React и Vue.*
2.  ✅ **Визуализация данных в реальном DOM** — Рендеринг таблицы из 2500 строк. *Реализовано для React и Vue.*
3.  ✅ **Внедрение системы в Svelte** — Применение тех же улучшений (FPS + таблица) для третьего фреймворка.
4.  ⏳ **Добавление режима "Оптимизированный рендеринг"** — Второй вариант теста с применением лучших практик оптимизации для каждого фреймворка.
5.  ⏳ **Сравнительный анализ и финальный отчёт** — Запуск серии тестов и составление итоговой таблицы сравнения.