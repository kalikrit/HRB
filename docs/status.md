# Статус проекта "Heavy Render Benchmark"

**Дата фиксации:** `2026-01-21`
**Цель:** Зафиксировать рабочее состояние MVP (Минимально жизнеспособного продукта) перед внесением сложных изменений в логику измерений.

## 🏗️ Архитектура

Работает **4 независимых сервиса**, развернутых локально:
1.  **Backend (FastAPI):** `http://localhost:8000`
    *   **Эндпоинт:** `POST /api/benchmark/start`
    *   **Генерация данных:** Конфигурируется (`payloadSize`, `complexity`).
    *   **CORS:** Настроен для фронтендов на портах 5173, 5174, 5175.
2.  **Frontend (React):** `http://localhost:5173`
3.  **Frontend (Vue 3):** `http://localhost:5174`
4.  **Frontend (Svelte):** `http://localhost:5175`

Все фронтенды обращаются к одному бэкенду за идентичными данными.

## 📦 Технологический стек

*   **Бэкенд:** Python 3.12, FastAPI, Pydantic, Uvicorn
*   **Фронтенды:** Node.js 22.x, Vite, Axios
    *   React (с хуками)
    *   Vue 3 (Composition API)
    *   Svelte (SvelteKit skeleton)
*   **ОС:** Ubuntu 24.04 LTS

## ✅ Реализованный функционал (MVP)

### Бэкенд
- [x] Генерация тестовых данных с тремя уровнями сложности (`low`, `medium`, `high`).
- [x] Валидация входящих запросов (ограничение `payloadSize: 1-5000`).
- [x] Настройка CORS для локальной разработки.

### Фронтенды (каждое приложение)
- [x] Базовая структура проекта (Vite).
- [x] Интерфейс с кнопкой запуска теста.
- [x] Отправка POST-запроса к бэкенду с параметрами:
    ```json
    { "framework": "...", "payloadSize": 1500, "complexity": "high" }
    ```
- [x] Отображение метрик:
    - Время получения данных (Network Time)
    - Время рендеринга (Render Time) — **ТРЕБУЕТ УТОЧНЕНИЯ (см. ниже)**
    - Общее время (Total Time)
    - Количество элементов
- [x] Отображение первых 5 элементов полученного массива в формате JSON.

## ⚠️ Известные проблемы и ограничения (Текущая версия)

1.  **Неточный замер времени рендеринга**
    *   **Проблема:** Используемые методы (`setTimeout`, `nextTick`, `tick`) фиксируют момент планирования обновления состояния, а не фактическую отрисовку DOM браузером.
    *   **Результат:** Метрика "Время рендеринга" показывает **0.3-0.8 мс** для 1500 элементов, что не отражает реальной производительности.
    *   **Пример:** React: `0.30 мс`, Vue: `0.80 мс`, Svelte: `0.50 мс`.

2.  **Отсутствие метрики FPS**
    *   Нет измерения плавности отрисовки (кадров в секунду), что критично для оценки пользовательского опыта при "тяжелом" рендеринге.

3.  **Данные не отрисовываются в реальном DOM**
    *   Фронтенды выводят только JSON-строку. Для честного теста нужна отрисовка полноценных DOM-элементов (таблицы, списка).

4.  **Нет режима "Оптимизированный рендеринг"**
    *   Все тесты выполняются в режиме "из коробки". Не реализован второй режим с применением оптимизаций (`React.memo`, `v-memo`).

## 📊 Последние полученные метрики (для справки)

*Параметры теста: `payloadSize=1500`, `complexity='high'`*

| Фреймворк | Время получения данных | Время рендеринга (неточно) | Общее время | Элементов |
| :--- | :--- | :--- | :--- | :--- |
| **React** | 87.20 мс | **0.30 мс** | 87.50 мс | 1500 |
| **Vue 3** | 72.60 мс | **0.80 мс** | 73.40 мс | 1500 |
| **Svelte** | 73.20 мс | **0.50 мс** | 73.70 мс | 1500 |

**Вывод по текущим метрикам:** Различия во "времени рендеринга" статистически незначимы и не отражают реальной производительности из-за указанной проблемы замера.

## 🚀 Следующие шаги (План улучшений)

1.  **Реализация точного замера рендеринга и FPS** — Использование `requestAnimationFrame` и `MutationObserver` для измерения реальной работы браузера.
2.  **Визуализация данных в реальном DOM** — Замена вывода JSON на рендеринг таблицы/списка DOM-элементов.
3.  **Добавление режима "Оптимизированный рендеринг"** — Второй вариант теста с применением лучших практик оптимизации для каждого фреймворка.
4.  **Увеличение нагрузки и вариативности тестов** — Тестирование с разными объемами данных и сценариями.