# Svelte + SSE Real-time Dashboard Prototype

**Цель проекта:** Практическая проверка гипотезы о том, что связка **SSE (Server-Sent Events)** и **Svelte** является оптимальным стеком для создания высокопроизводительных дашбордов с обновлением данных в реальном времени.

## 🎯 Задачи прототипа

1.  **Backend (FastAPI):** Создать эндпоинт `/api/stream`, генерирующий непрерывный поток тестовых данных в формате SSE.
2.  **Frontend (Svelte):** Реализовать подключение к SSE-потоку, эффективное обновление состояния (store) и отображение данных.
3.  **Бенчмарк:** Измерить ключевые метрики производительности:
    *   **Задержка (Latency):** Время между генерацией события на сервере и его отрисовкой в браузере.
    *   **Плавность (FPS):** Стабильность кадровой частоты при интенсивном потоке обновлений.
    *   **Удобство разработки (DX):** Оценить простоту и лаконичность кода для подобных задач.

## 🏗️ Архитектура

```
[FastAPI Backend (:8000)]
|
| SSE Stream (text/event-stream)
v
[Svelte Frontend (:5176)]
|
|---> EventSource listener
|---> Svelte Store update
`---> Reactive UI update
```

## 📁 Структура проекта

Проект является частью монорепозитория `heavy-render-benchmark` и расположен в:
`frontends/svelte-sse-test/`

*   `src/routes/+page.svelte` - главная и единственная страница прототипа.
*   `src/lib/stores.js` - модуль с хранилищами (stores) состояния приложения.
*   `README-SSE-TEST.md` - эта документация.

## 🚀 Запуск

1.  **Запустите бэкенд:** Из директории `backend` выполните `uvicorn main:app --reload`.
2.  **Запустите фронтенд:** Из этой директории выполните `npm run dev`.
3.  **Откройте в браузере:** Адрес будет выведен в терминале (обычно `http://localhost:5176`).

## 🔬 Методология тестирования

Производительность оценивается по двум направлениям:

1.  **Субъективно:** Визуальная оценка плавности анимации (движение маркеров, обновление чисел).
2.  **Объективно:** Замер FPS с помощью встроенного модуля бенчмаркинга, аналогичного использованному в основном проекте.

## 📈 Ожидаемый результат

На основе результатов этого прототипа будет сделан вывод, подтверждающий или опровергающий тезис о том, что **Svelte в связке с SSE обеспечивает наивысшую плавность (FPS) рендеринга для дашбордов реального времени** по сравнению с React и Vue в аналогичных условиях.

---
*Проект создан в рамках исследования "Heavy Render Benchmark".*